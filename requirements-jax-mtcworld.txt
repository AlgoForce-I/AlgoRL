# Optional: JAX-native MetaWorld / Continual World (GPU + mujoco-mjx Warp).
# Install the library from source until it is published on PyPI, e.g.:
#   pip install git+https://github.com/AlgoForce-I/MTCWorldMJX
MTCWorldMJX>=0.1.0
