"""Optax optimizer wrappers for the JAX backend.

Implement helpers here to build optimizers and learning-rate schedules, for example:
- ``create_optimizer(lr, ...)`` returning an Optax gradient transformation
- optional warmup / cosine decay schedules shared across learners
"""
